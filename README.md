#Medical data migration

Python ETL pipeline to extract, clean, and load data into MongoDB. Features Docker containerization for consistent deployment and optional AWS integration for cloud storage. Includes modular structure, config management, and tests.
